import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.brightraccoonnap',
  appName: 'bright-raccoon-nap',
  webDir: 'dist'
};

export default config;
